use std::fs;
use std::path::Path;
use serde_json;
use crate::vault::Vault;

pub struct Storage;

impl Storage {
    /// Load vault from a JSON file
    pub fn load_vault() {
        todo!();
    }

    /// Save vault to a JSON file
    pub fn save_vault() {
        todo!();
    }
}