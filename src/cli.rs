// Clap definitions for CLI
use clap::{Parser, Subcommand};

// [derive(Parser)]
// [derive(Subcommand)]

/* 
Goal: convert terminal input → structured enums 
command, about, version
subcommands: init, add, get, list
*/